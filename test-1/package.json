{
  "name": "test-1",
  "version": "1.0.0",
  "description": "typescript test-1",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "ts": "tsc --project tsconfig.json",
    "ts:clean": "tsc --build --clean",
    "ts:config": "tsc --showConfig --project tsconfig.json",
    "ts:declarations": "tsc --project tsconfig.json --declaration --emitDeclarationOnly"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
